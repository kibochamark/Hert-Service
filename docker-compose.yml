services:
  hert-service:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: hert-service
    ports:
      - "4000:4000"
    env_file:
      - .env
    restart: unless-stopped
